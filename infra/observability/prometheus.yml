global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: voice-agent
    metrics_path: /metrics
    static_configs:
      - targets:
          - voice-agent:3000
